import { createTheme, ThemeProvider } from '@mui/material/styles';

export const ButtonColour = createTheme({
  palette: {
    primary: {
      main: '#aeb0b3', // Replace with your custom color
    },
  },
});